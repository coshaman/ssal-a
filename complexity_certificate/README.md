# Complexity certificate

`blockwise_sample.json` is a machine-readable instrumentation certificate for the single-run fast solver. It records distinct block creations and later cache hits, demonstrating the all-`k` reuse invariant on a representative run. It is evidence for the implementation invariant, not by itself a proof of the global `O(n^5 M(n))` theorem.
