# Artifact consistency audit

Date: 2026-09-03.

Authoritative state: `PROOF_INCOMPLETE` in `FINAL_STATUS.md`; the surviving scientific scope is counting-only and is being evaluated under the specialist-journal continuation.

Authoritative verification run: `python scripts/verify_all.py` => 34 main tests and 10 mutation tests passed; Python compilation and certificate generation passed. The block certificate is `complexity_certificate/blockwise_sample.json`, with `n=8`, `omega=8`, 406 block creations, and 722 cache hits.

The current theorem is modular `O(n^5 M_p(n))` field operations plus a separate exact CRT corollary. The unconditional bit/RAM improvement and sampler are not claimed. The dissertation's regrouping is prior art; priority language remains conservative. The manuscript is `manuscript/main.tex`; PDF compilation and visual QA are complete, author metadata is present, but the fresh unanimous review gate failed. Historical reports are labeled or superseded; no current status file is authorized to claim `MANUSCRIPT_READY`, `SUBMISSION_READY`, or release approval.
