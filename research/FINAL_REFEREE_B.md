# Final referee B — computer algebra / FPS

Recommendation: **MINOR REVISION**.

The EGF normalization has the correct `(q-1)!` shift, the endpoint `ell'=ell` is separated from the self-reference, and truncation is stated at the block length. The modular theorem requires `p>n`, making the factorial denominators invertible. The exact CRT corollary is valid because only the final integer count is reconstructed.

The paper must label the `tilde O(n^8)` discussion as conditional, cite standard arbitrary-field fast multiplication, and state regularity assumptions on `M_p` explicitly. These are presentation revisions, not fatal mathematical objections.
