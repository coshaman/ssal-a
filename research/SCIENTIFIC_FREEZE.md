# Scientific freeze

Freeze date: 2026-09-03.

The authoritative result is a counting-only theorem. The HLV recurrence is the source; the dissertation's `r=x'+ell'` and six-argument `h` regrouping are prior art. The present acceleration is the additional coefficient-extraction convolution plus EGF/FPS linear-ODE solution, evaluated once for all `k` in each block. For every prime `p>n`, the modular algorithm computes `C(n,omega) mod p` in `O(n^5 M_p(n))` field operations under the stated FPS regularity assumptions. Exact nonnegative integer recovery follows from sufficiently many pairwise-coprime primes and CRT. The unrestricted case is `omega=n`.

No unconditional improved RAM/bit bound, sampler theorem, or absolute priority claim is made. The sampler is out of scope. The dissertation regrouping is credited explicitly. The scientific core is being evaluated under the current specialist-journal standard; the old selective algorithms/TCS stress-test outcome is historical and does not control this gate.
